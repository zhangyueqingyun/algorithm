const getDirFiles = require('./src/getDirFiles');
const processInput = require('./src/processInput');
const tempFile = require('./src/tempFile');

module.exports = {
    getDirFiles,
    processInput,
    tempFile
};