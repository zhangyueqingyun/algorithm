const Node = require('./Node');
class SubMinHeap {
    constructor(root, degree, next) {
        this.root = root;
        this.degree = degree;
        this.next = next;
    }
}

module.exports = SubMinHeap;